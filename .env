# Local defaults. CI overrides these with the deployed GitHub Pages URLs, so the
# application switcher points at whichever environment it is running in.
PUBLIC_APP_BOT_STUDIO=http://localhost:5174/
PUBLIC_APP_MONITORING=http://localhost:5175/
PUBLIC_APP_ORGANIZATION=http://localhost:5176/
